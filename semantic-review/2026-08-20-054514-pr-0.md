# Independent steering oracle and deterministic grounding fallback for Wave 1

This pass rechecks the three findings from the prior Wave 1 review. Property 11 now derives expected steering curvature with an independent piecewise-linear oracle for every generated speed/steer pair, while grounding now enforces deterministic hit order, rejects invalid normals, and retains the first valid ordered normal when aggregation degenerates. Properties 23, 10, and 19 retain their prior conclusions under focused validation. The task plan still attributes Requirement 10.10 to Core grounding and Property 13 even though that obligation belongs to the deferred typed feature-status record. **Watch for:** **Low · confirmed** — stale Requirement 10.10 claims remain in `.kiro/specs/rocket-arena/tasks.md:287,320-323` and should be removed in favor of the deferred Feature Status Record owner.

**Verdict**: NEEDS_CHANGES

## High-level view

Property 11 now compares controller output with a test-local interpolation of the configured `Steering_Curvature_Curve` across positive and negative steering, generated speeds, and exact 5, 14.1, and 23 m/s scenarios. Literal assertions anchor the 0, 5, 14.1, and 23 m/s curve knots, so a speed-independent curvature implementation fails the generated comparisons.

Production grounding canonicalizes accepted hits by contact-point index and collider handle before aggregation. Non-finite and zero normals are excluded, a cancelling sum resolves to the first valid normal in canonical order, Property 13 observes returned order directly, and the legacy harness no longer treats semantic-ID order as behavior.

**Low · confirmed:** the Property 13 source annotation omits Requirement 10.10 and Task 9.1 assigns 10.10 to `docs/feature-status.json` validation, but Tasks 5.5 and 5.10 still claim Core grounding and Property 13 satisfy it. Those stale references leave the task ledger inconsistent with the corrected evidence boundary.

Properties 23, 10, and 19 are behaviorally isolated from the grounding-order change, while Property 10 still exercises the production boost accessor. Eight targeted tests across all six Wave 1 property suites and shared/server typechecks passed without changing the prior collision, controller, finite-recovery, inventory, or authority conclusions.

<details>
<summary>Issues (1)</summary>

1. **Stale Requirement 10.10 ownership — Low · confirmed** — `.kiro/specs/rocket-arena/tasks.md:287,320-323` still assigns the typed Feature Status Record obligation to Core grounding and Property 13. Remove 10.10 from those claims and leave it with the explicit deferred Advanced-surface status-record work.

</details>

<details>
<summary>Details</summary>

## Steering curvature is now checked outside the controller implementation

`server/src/physics/grip-handbrake.property.test.ts:154-172` implements a test-local piecewise-linear evaluator over `StructuredCurve.samples`. Every generated case computes `steer * oracle(curve, abs(forwardSpeed))` and compares it with both normal and handbrake base curvature at lines 599-614. The matrix spans forward/reverse movement, varied nonzero steering, generated speeds, and configured boundaries; literal-knot assertions at lines 675-690 keep the oracle anchored independently of controller output.

## Support ordering and degenerate aggregation share one deterministic contract

`server/src/physics/grounding.ts:130-136` sorts accepted hits by contact-point index and collider handle, with distance and semantic ID only as later ties. `normalizeFinite` excludes non-finite and zero-length hit normals before thresholding, while `combineGroundingNormals` records the first valid ordered normal and uses it when the normalized sum is zero or invalid.

Property 13 does not re-sort `acceptedHits` when building traces. It requires raw indices `[0,1,2,3]` and directly asserts cancelling, invalid-leading, and invalid-only normal sets at `server/src/physics/grounding.property.test.ts:1148-1175`. The legacy harness checks contact-index order and sorts only a copied surface-ID set for membership.

## Requirement 10.10 remains assigned to grounding evidence

**Low · confirmed.** Requirement 10.10 requires a `Feature_Status_Record` identifying every deferred Advanced surface. Property 13 correctly lists only 10.1-10.5 and 10.11-10.12 at `server/src/physics/grounding.property.test.ts:1178-1180`, and Task 9.1 assigns 10.10 to future executable status/release validation at `.kiro/specs/rocket-arena/tasks.md:519-525`.

The task plan nevertheless retains 10.10 under completed Core grounding at `.kiro/specs/rocket-arena/tasks.md:287` and under Property 13 at lines 320-323. Removing those references and making the Advanced-surface deferral/status-record task the sole owner resolves the remaining overclaim.

## Regression-sensitive proof boundaries

Property 23 observes collision manifolds and pre-recovery ball spin in independent Rapier worlds without support aggregation. Property 10 drives controller observations directly and reaches the production Rapier room for finite/clamped boost publication. Property 19 uses paired fixture worlds with a fixed grounding result, so its forged-field/control-only comparison does not depend on production hit ordering. Property 9’s throttle oracle is unchanged and its exact-boundary tests remain green.

Validation completed with eight tests passed and zero failed across Properties 9, 11, 13, 23, 10, and 19. `tsc --noEmit` passed for `shared` and `server`, and `git diff master --check` passed for the scoped tracked files.

</details>

<details>
<summary>File map</summary>

- `server/src/physics/physics-construction.property.test.ts` — retained Property 23 collision-owned-spin proof.
- `server/src/physics/throttle.property.test.ts` — retained Property 9 throttle and exact-boundary proof.
- `server/src/physics/grip-handbrake.property.test.ts` — independent steering-curve oracle and exact knot checks.
- `server/src/physics/grounding.ts` — deterministic hit ordering, malformed-normal rejection, and first-valid fallback.
- `server/src/physics/grounding.property.test.ts` — raw hit-order and cancellation/invalid aggregation coverage.
- `server/src/physics/test-grounding.ts` — contact-index ordering without semantic-ID-first behavior.
- `server/src/physics/finite-bounds.property.test.ts` — retained Property 10 finite recovery and inventory proof.
- `server/src/rooms/server-authority.property.test.ts` — retained Property 19 authority-isolation proof.
- `server/src/rooms/rapier-room-world.ts` — finite/clamped boost storage exercised by Property 10.
- `.kiro/specs/rocket-arena/tasks.md` — stale Requirement 10.10 ownership claims.

Full scoped diff: `git diff master -- server/src/physics/grounding.ts server/src/physics/test-grounding.ts server/src/rooms/rapier-room-world.ts .kiro/specs/rocket-arena/tasks.md`; the six property suites are untracked additions and should be reviewed as full-file diffs.

</details>
